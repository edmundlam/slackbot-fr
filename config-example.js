/**
 * Example config file for the slackbot.
 * Copy this file to config.js and set up your token
 * before running the bot. 
**/

var config = {};

config.token = 'Your token here'; // Add a bot https://my.slack.com/services/new/bot and put the token 
config.name = 'My Bot'

module.exports = config;